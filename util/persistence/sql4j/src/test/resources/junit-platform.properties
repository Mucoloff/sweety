# Parallel method execution is disabled because tests share global static state
# (SqlRunner.logger, SqlRunner.slowQueryThresholdMs) and per-class SQLite DBs.
# Concurrent execution causes false positives in query-count assertions.
junit.jupiter.execution.parallel.enabled = false
